const assert = require('chai').assert;
const checker = require('./beer-o-clock-checker');

describe("The beer-o-clock checker", () =>
    it("Knows when it is beer-o-clock", () => {
        assert.fail("TODO: write specs for the beer o clock checker!")
    }
));