const assert = require('chai').assert;
const calculator = require('./calculator');

describe("The calculator", () =>{
    it("Can add", () => {
        assert.fail("TODO: write specs");
    })

    it("Can multiply", () => {
        assert.fail("TODO: write specs");
    })
});